export const entriesQueryKey = (month: string) => ["entries", month];

export const dashboardSummaryQueryKey = (month: string) => ["dashboard-summary", month];

export const planningQueryKey = (month: string) => ["planning", month];

export const cardsSummaryQueryKey = () => ["cards-summary"];
